import CrearEvento from './CrearEvento';

export const metadata = {
  title: 'Crear Evento',
  description: 'Pantalla para crear nuevos eventos con detalles completos.',
};

export default function Page() {
  return <CrearEvento />;
}
