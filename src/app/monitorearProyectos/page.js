import MonitorearProyectos from './MonitorearProyectos';

export const metadata = {
  title: 'Monitorear Proyectos',
  description: 'Página para monitorear los proyectos existentes',
};

export default function MonitorearProyectosPage() {
  return <MonitorearProyectos />;
}
