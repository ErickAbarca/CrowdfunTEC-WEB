import MenuUsuario from './menuUsuario';

export const metadata = {
  title: 'Menú Usuario',
  description: 'Pantalla principal para los usuarios',
};

export default function MenuUsuarioPage() {
  return <MenuUsuario />;
}
